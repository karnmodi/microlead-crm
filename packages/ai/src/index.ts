/**
 * Prompt builders and provider-agnostic contracts.
 * Wire providers in apps/api (see planning phase-h).
 */
export function placeholderPrompt(): string {
  return "Define prompts when implementing AI module.";
}
